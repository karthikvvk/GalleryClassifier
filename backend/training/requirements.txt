# ─────────────────────────────────────────────
# GalleryClassifier  —  Training requirements
# Install on Colab with: pip install -r requirements.txt
# ─────────────────────────────────────────────

# Core deep learning
torch>=2.1.0
torchvision>=0.16.0

# Data processing
Pillow>=10.0.0
imagehash>=4.3.1    # perceptual hashing for dedup

# Metrics
scikit-learn>=1.3.0

# ONNX export & verification
onnx>=1.15.0

# Utilities
tqdm>=4.66.0        # progress bars (optional, not used in code but handy in Colab)
